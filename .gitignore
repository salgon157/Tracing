# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# --- Testy / cache ---
.pytest_cache/

# --- DATA (cely strom) — NIKDY do gitu ---
# Data zijou VEDLE repa: Tracing_Main/{vrp_benchmark, data, UI}.
# Kdyby si nekdo slozku 'data' uvnitr repa lokalne vyrobil, tohle je pojistka.
# Obsahuje osobni udaje zakazniku i ridicu (GDPR): riro vstupy, prepared
# objednavky, locations_*.csv, registr aut+ridicu, historie zavozu.
# Verzuje se JEN kod — vcetne closures.json a vehicle_types (od 21.8.2026
# take provozni data, viz WORKFLOW: Nasazeni na server).
/data/
benchmark/results/
*.log
benchmark_results.csv
benchmark_summary.txt

# --- Routovaci data (ORS/OSRM grafy, stahovana / v Docker volume) ---
routing/

# --- Lokalni nastaveni editoru / Claude ---
.claude/settings.local.json
.vscode/

# --- webui runtime joby (job.json + job.log per beh) ---
webui/jobs/

# --- vystupy experimentu (generovane, muzou obsahovat PII z objednavek) ---
experiments/*/results/

# DOČASNÝ ARCHIV — NESAHAT: git worktree se starým solverem (strana A
# regresního A/B, viz regression_ab.py a WORKFLOW 8). overnight_regression.ps1
# ho založí jen pro běh a na konci odstraní. Není součást projektu, needituje
# se, nespouští se odsud ostrý běh. Popis: _baseline_*/_NESAHAT_ARCHIV.md
/_baseline_*/
