# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# --- Testy / cache ---
.pytest_cache/

# --- DATA (cely strom) — NIKDY do gitu ---
# Data zijou VEDLE repa: Tracing_Main/{vrp_benchmark, data, UI}.
# Kdyby si nekdo slozku 'data' uvnitr repa lokalne vyrobil, tohle je pojistka.
# Obsahuje osobni udaje zakazniku i ridicu (GDPR): riro vstupy, prepared
# objednavky, locations_*.csv, registr aut+ridicu, historie zavozu.
# Verzuje se JEN kod — vcetne closures.json a vehicle_types (od 21.8.2026
# take provozni data, viz WORKFLOW: Nasazeni na server).
/data/
*.log

# Vystupy benchmarku, experimentu a webui jobu chodi pod ../data/ (viz paths.py).
# Tyhle radky jsou uz jen pojistka pro starsi pracovni kopie.
benchmark/results/
benchmark_results.csv
benchmark_summary.txt
webui/jobs/
experiments/*/results/

# --- Routovaci data (ORS/OSRM grafy, stahovana / v Docker volume) ---
routing/

# --- Lokalni nastaveni editoru / Claude ---
.claude/settings.local.json
.vscode/

# POJISTKA: baseline worktree pro regresni A/B se zaklada v %TEMP%, ne tady
# (vypis stareho commitu nese tehdejsi data/static -- ve slozce s kodem
# nesmi lezet zadna data). Kdyby ho nekdo presto zalozil v repu, at aspon
# neni v gitu. Viz regression_ab.py a WORKFLOW sekce 8.
/_baseline_*/
